<%- include('../partials/head', { title: title }) %>
<%- include('../partials/header') %>
<div class="container" style="padding-top: 24px; padding-bottom: 24px;">
    <h1 style="margin-bottom: 16px;">Каталог автомобилей</h1>
    <form class="filters" method="get" action="/cars">
        <div class="form-group">
            <label for="start_date">Дата начала</label>
            <input type="date" class="form-control" id="start_date" name="start_date" value="<%= filters.start_date %>">
        </div>
        <div class="form-group">
            <label for="end_date">Дата окончания</label>
            <input type="date" class="form-control" id="end_date" name="end_date" value="<%= filters.end_date %>">
        </div>
        <button type="submit" class="btn">Показать доступные</button>
    </form>

    <% if (models.length > 0) { %>
        <div class="grid">
            <% models.forEach(model => { %>
                <div class="card">
                    <div class="card-title"><%= model.brand %> <%= model.model %></div>
                    <div class="card-text">
                        Кузов: <%= model.body_type %>, 
                        КПП: <%= model.transmission === 'automatic' ? 'Автомат' : 'Механика' %>
                    </div>
                    <div class="card-price"><%= model.base_price_per_day.toLocaleString('ru-RU') %> ₽ / сутки</div>
                    <a href="/cars/<%= model.id %>" class="btn" style="margin-top:8px;">Подробнее</a>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <p>Нет доступных автомобилей на выбранные даты.</p>
    <% } %>
</div>
<%- include('../partials/footer') %>